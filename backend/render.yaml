services:
  - type: web
    name: grammar-backend
    env: python
    workingDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.3
